apiVersion: project-arbiter.io/v1alpha1
kind: Project
metadata:
  name: tenant-a
spec:
  tenantID: ab
  # targetNamespace: tenant-ab   # optional; defaults to tenant-<tenantID>
  # targetNamespaces:            # optional; defaults to ["tenant-<tenantID>"]
  #   - tenant-ab
  #   - tenant-ops
  # adminSubjects: []            # optional
  # baselinePolicy: {}           # optional
