apiVersion: project-arbiter.io/v1alpha1
kind: Project
metadata:
  name: project-a
spec:
  tenantID: ab
  # targetNamespace: project-ab   # optional; defaults to project-<tenantID>
  # targetNamespaces:             # optional; defaults to ["project-<tenantID>"]
  #   - project-ab
  #   - project-ops
  # adminSubjects: []            # optional
  # baselinePolicy: {}           # optional
