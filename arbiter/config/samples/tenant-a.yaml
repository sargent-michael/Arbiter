# config/samples/arbiter_v1alpha1_settler.yaml (or a new file)
apiVersion: project-arbiter.io/v1alpha1
kind: Settler
metadata:
  name: hawkins
spec:
  settlerID: "hawkins"
  # targetNamespace: "settler-hawkins"  # optional; default is settler-<settlerID>
  # targetNamespaces:              # optional; defaults to ["settler-<settlerID>"]
  #   - hawkins-lab
  #   - hawkins-ops
  baselinePolicy:
    networkIsolation: true
    resourceQuota: true
    limitRange: true
  adminSubjects:
    - kind: User
      name: "eleven@hawkins.local"
