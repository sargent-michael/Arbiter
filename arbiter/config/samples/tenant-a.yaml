# config/samples/arbiter_v1alpha1_occupant.yaml (or a new file)
apiVersion: project-arbiter.io/v1alpha1
kind: Occupant
metadata:
  name: hawkins
spec:
  occupantID: "hawkins"
  # targetNamespace: "occupant-hawkins"  # optional; default is occupant-<occupantID>
  # targetNamespaces:              # optional; defaults to ["occupant-<occupantID>"]
  #   - hawkins-lab
  #   - hawkins-ops
  baselinePolicy:
    networkIsolation: true
    resourceQuota: true
    limitRange: true
  adminSubjects:
    - kind: User
      name: "eleven@hawkins.local"
