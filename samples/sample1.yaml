apiVersion: project-arbiter.io/v1alpha1
kind: Occupant
metadata:
  name: starcourt
spec:
  occupantID: starcourt

  # Defaults apply (namespace name, baseline controls)
  adminSubjects:
    - kind: Group
      name: scoops-ahoy-crew
