apiVersion: project-arbiter.io/v1alpha1
kind: Occupant
metadata:
  name: hawkins-rd
spec:
  occupantID: hawkins-rd

  # Multiple namespaces managed by a single occupant.
  targetNamespaces:
    - hawkins-rd
    - starcourt-mall
    - radio-room

  adminSubjects:
    - kind: Group
      name: hawkins-lab-ops
    - kind: User
      name: hopper@hawkins.local

  # Per-occupant overrides on top of the Baseline defaults.
  baselinePolicy:
    allowedIngressPorts:
      - 443
      - 8443
    resourceQuotaSpec:
      hard:
        requests.cpu: "4"
        requests.memory: 8Gi
        limits.cpu: "8"
        limits.memory: 16Gi
        pods: "100"
    limitRangeSpec:
      limits:
        - type: Container
          defaultRequest:
            cpu: 250m
            memory: 256Mi
          default:
            cpu: "1"
            memory: 1Gi
