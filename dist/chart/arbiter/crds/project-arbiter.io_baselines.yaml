---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: baselines.project-arbiter.io
spec:
  group: project-arbiter.io
  names:
    kind: Baseline
    listKind: BaselineList
    plural: baselines
    shortNames:
    - bl
    singular: baseline
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Baseline defines default baseline policy settings for tenants.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
            type: string
          metadata:
            type: object
          spec:
            description: BaselineSpec defines the desired state of Baseline.
            properties:
              baselinePolicy:
                description: baselinePolicy provides default settings for all tenants.
                properties:
                  allowedIngressPorts:
                    description: |-
                      allowedIngressPorts overrides the default allowed ingress ports (TCP).
                      If empty, defaults to [443].
                    items:
                      format: int32
                      type: integer
                    type: array
                    x-kubernetes-list-type: atomic
                  limitRange:
                    description: apply default LimitRange
                    type: boolean
                  limitRangeSpec:
                    description: limitRangeSpec overrides the default LimitRange spec when set.
                    properties:
                      limits:
                        description: Limits is the list of LimitRangeItem objects
                          that are enforced.
                        items:
                          description: LimitRangeItem defines a min/max usage limit
                            for any resource that matches on kind.
                          properties:
                            default:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                                x-kubernetes-int-or-string: true
                              description: Default resource requirement limit value
                                by resource name if resource limit is omitted.
                              type: object
                            defaultRequest:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                                x-kubernetes-int-or-string: true
                              description: DefaultRequest is the default resource
                                requirement request value by resource name if resource
                                request is omitted.
                              type: object
                            max:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                                x-kubernetes-int-or-string: true
                              description: Max usage constraints on this kind by
                                resource name.
                              type: object
                            maxLimitRequestRatio:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                                x-kubernetes-int-or-string: true
                              description: MaxLimitRequestRatio, if specified, imposes
                                a max limit to request ratio on this kind by resource
                                name.
                              type: object
                            min:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                                x-kubernetes-int-or-string: true
                              description: Min usage constraints on this kind by
                                resource name.
                              type: object
                            type:
                              description: Type of resource that this limit applies
                                to.
                              type: string
                          required:
                          - type
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                  networkIsolation:
                    description: enable default deny ingress/egress with DNS egress
                      and HTTPS ingress allowed
                    type: boolean
                  resourceQuota:
                    description: apply default ResourceQuota
                    type: boolean
                  resourceQuotaSpec:
                    description: resourceQuotaSpec overrides the default ResourceQuota
                      spec when set.
                    properties:
                      hard:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))([eE](\+|-)?[0-9]+)?$
                          x-kubernetes-int-or-string: true
                        description: hard is the set of desired hard limits for each
                          named resource.
                        type: object
                      scopeSelector:
                        description: scopeSelector is also a collection of filters
                          like scopes that must match each object tracked by a quota
                          but expressed using ScopeSelectorOperator in combination
                          with possible values.
                        properties:
                          matchExpressions:
                            description: A list of scope selector requirements by
                              scope of the resources.
                            items:
                              description: A scoped-resource selector requirement
                                is a selector that contains values, a scope name,
                                and an operator that relates the scope name and values.
                              properties:
                                operator:
                                  description: Represents a scope's relationship
                                    to a set of values.
                                  type: string
                                scopeName:
                                  description: The name of the scope that the selector
                                    applies to.
                                  type: string
                                values:
                                  description: An array of string values.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - operator
                              - scopeName
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      scopes:
                        description: |-
                          A collection of filters that must match each object tracked by a quota.
                          If not specified, the quota matches all objects.
                        items:
                          description: A ResourceQuotaScope defines a filter that
                            must match each object tracked by a quota
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                    type: object
                type: object
            type: object
          status:
            description: BaselineStatus defines the observed state of Baseline.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        This may be empty.
                      maxLength: 1024
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
